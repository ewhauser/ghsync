version: "2"
sql:
  - engine: "postgresql"
    queries: "db/queries"
    schema: "db/migrations"
    gen:
      go:
        package: "dbgen"
        out: "internal/store/dbgen"
        sql_package: "pgx/v5"
        emit_json_tags: false
        emit_interface: false
